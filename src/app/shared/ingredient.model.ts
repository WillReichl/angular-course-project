export class Ingredient {
    // TypeScript shortcut for listing out public properties individually and assigning them in the construction using this.XYZ = XYZ
    constructor (public name: string, public amount: number) { }
}
